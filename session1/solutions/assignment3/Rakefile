namespace :files do
  desc 'Rename files named *.yaml to be *.yml'
  task :rename_yml, [:path] do |t, args|
    path = args.path || Dir.pwd
    files = Dir.glob(path + "/*.{yaml}").each do |f|
      prefix = f.split("/").last
      prefix = prefix.split(".").first
      File.rename("#{f}", "#{path}/#{prefix}.yml")
    end
  end
end
